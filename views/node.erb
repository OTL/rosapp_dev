<table class="table table-striped table-boarderd">
<thead>
  <tr><th>node name</th><th>uri</th></tr>
</thead>
<tbody>
<% @nodes.each do |node, uri| %>
<tr><td>
<div class="btn-group">
  <button class="btn <%= if node=='/rosout' then 'btn-inverse' else 'btn-primary' end %> dropdown-toggle" data-toggle="dropdown"><%= node %> <span class="caret"></span></button>
  <ul class="dropdown-menu">
    <li><a href="#"><i class="icon-remove"></i>kill</a></li>
  </ul>
</div>
</td></tr>
<% end %>
</tbody>
<table>
